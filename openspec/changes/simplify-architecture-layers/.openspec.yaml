schema: spec-driven
created: 2026-09-23
skip_specs: true
# Structural / contracts docs — no product behavior deltas; no src until contracts gate.
goal: >-
  Approve product map, document layer/domain contracts (Transport facade + Codec),
  then only incremental leak cleanup — no code until contracts agreed
affected_areas:
  - docs/architecture.md
  - src/lib
  - src/domain
  - src/packages/signaling
