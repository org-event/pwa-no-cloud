schema: spec-driven
created: 2026-09-23
skip_specs: true
# Pure structural / dependency-rule change — no product behavior deltas.
goal: Document product-facing client map (UI → API → Domain blocks → Transport → Platform) and incremental leak cleanup without rewriting the P2P hub
affected_areas:
  - docs/architecture.md
  - src/lib
  - src/domain
  - src/packages/signaling
